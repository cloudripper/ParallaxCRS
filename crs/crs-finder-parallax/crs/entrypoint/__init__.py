"""Run-phase entrypoints, selected by entrypoint.sh via $CRS_ENTRYPOINT.

`run_crs` is the supported bug-finding strategy. The other upstream strategies
and probes are available only with an explicit experimental-entrypoint opt-in.
Build-phase scripts live in the top-level builder/ directory.
"""
