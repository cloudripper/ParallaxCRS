"""Framework Python for the CRS: the Claude Code LangGraph node (claude_node),
the run-phase bootstrap/lifecycle (runtime), and prompt construction (prompts).

The prompt ASSETS these render/install — CLAUDE.md.j2, skills/, agents/, roles/ —
live at the crs/ package root (one level up), not here.
"""
