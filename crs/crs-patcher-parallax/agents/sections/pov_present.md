## POV Variants

{pov_count} proof-of-vulnerability input(s) were provided:

{pov_list}

You can validate whether an input triggers a crash with `libCRS run-pov`. Use `--rebuild-id` from a prior `apply-patch-build` to test a patched build, or omit it to test against the original unpatched base build.
Your patch must fix all POV variants — verify each one before submission.
